'use strict';const express=require('express');const {requireAuth,requireDeveloper}=require('../../middleware/auth');const {requireWorkspace}=require('../../core/security/workspace');const c=require('./environments.controller');const r=express.Router();r.use(requireAuth,requireWorkspace);r.get('/app/:id',c.index);r.post('/app/:id',requireDeveloper,c.setVar);r.post('/app/:id/:name/delete',requireDeveloper,c.removeVar);module.exports=r;
