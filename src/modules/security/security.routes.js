'use strict';const express=require('express');const rateLimit=require('express-rate-limit');const {requireAuth}=require('../../middleware/auth');const {requireWorkspace}=require('../../core/security/workspace');const c=require('./security.controller');const r=express.Router();const sensitive=rateLimit({windowMs:15*60*1000,limit:12,standardHeaders:'draft-8',legacyHeaders:false});r.use(requireAuth,requireWorkspace);r.get('/',c.index);r.post('/2fa/begin',sensitive,c.begin2fa);r.post('/2fa/enable',sensitive,c.enable2fa);r.post('/2fa/disable',sensitive,c.disable2fa);r.post('/sessions/revoke-others',sensitive,c.revokeOthers);r.post('/sessions/:id/revoke',sensitive,c.revokeSession);r.post('/tokens',sensitive,c.createToken);r.post('/tokens/:id/revoke',sensitive,c.revokeToken);module.exports=r;
