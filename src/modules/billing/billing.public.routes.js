'use strict';const express=require('express');const rateLimit=require('express-rate-limit');const midtrans=require('./midtrans.service');const r=express.Router();const limiter=rateLimit({windowMs:60*1000,limit:120,standardHeaders:'draft-8',legacyHeaders:false});r.post('/',limiter,express.json({limit:'128kb'}),async(req,res,next)=>{try{const result=await midtrans.processNotification(req.body||{});res.status(200).json({ok:true,duplicate:Boolean(result.duplicate)});}catch(e){next(e);}});module.exports=r;
