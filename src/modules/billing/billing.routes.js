'use strict';const express=require('express');const rateLimit=require('express-rate-limit');const {requireAuth,requirePlatformAdmin,requireAdmin}=require('../../middleware/auth');const {requireWorkspace}=require('../../core/security/workspace');const c=require('./billing.controller');const r=express.Router();const pay=rateLimit({windowMs:10*60*1000,limit:20,standardHeaders:'draft-8',legacyHeaders:false});r.use(requireAuth,requireWorkspace);r.get('/',requireAdmin,c.index);r.post('/checkout',requireAdmin,pay,c.checkout);r.post('/recurring',requireAdmin,pay,c.recurring);r.post('/recurring/toggle',requireAdmin,pay,c.recurringToggle);r.post('/transactions/:id/reconcile',requireAdmin,pay,c.reconcile);r.post('/transactions/:id/cancel',requireAdmin,pay,c.cancel);r.post('/transactions/:id/refund',requirePlatformAdmin,pay,c.refund);r.post('/plans',requirePlatformAdmin,c.savePlan);r.post('/subscription',requirePlatformAdmin,c.subscription);r.post('/invoice',requirePlatformAdmin,c.invoice);r.post('/invoices/:id/paid',requirePlatformAdmin,c.paid);module.exports=r;
