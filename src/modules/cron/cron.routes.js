'use strict';const express=require('express');const {requireAuth,requireDeveloper}=require('../../middleware/auth');const {requireWorkspace}=require('../../core/security/workspace');const c=require('./cron.controller');const r=express.Router();r.use(requireAuth,requireWorkspace);r.get('/app/:id',c.index);r.post('/app/:id',requireDeveloper,c.create);r.post('/app/:id/:jobId/delete',requireDeveloper,c.remove);module.exports=r;
