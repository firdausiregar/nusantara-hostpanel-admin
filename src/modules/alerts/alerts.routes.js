'use strict';const express=require('express');const {requireAuth,requireAdmin,requireDeveloper}=require('../../middleware/auth');const {requireWorkspace}=require('../../core/security/workspace');const c=require('./alerts.controller');const r=express.Router();r.use(requireAuth,requireWorkspace);r.get('/',requireDeveloper,c.index);r.post('/',requireAdmin,c.create);r.post('/:id/delete',requireAdmin,c.remove);module.exports=r;
