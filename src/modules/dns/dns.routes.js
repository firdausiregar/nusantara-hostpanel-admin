'use strict';const express=require('express');const {requireAuth,requireAdmin,requireDeveloper}=require('../../middleware/auth');const {requireWorkspace}=require('../../core/security/workspace');const c=require('./dns.controller');const r=express.Router();r.use(requireAuth,requireWorkspace);r.get('/',c.index);r.post('/',requireAdmin,c.createZone);r.get('/:id',c.detail);r.post('/:id/records',requireDeveloper,c.addRecord);r.post('/:zoneId/records/:recordId/delete',requireDeveloper,c.deleteRecord);r.post('/:id/wildcard-ssl',requireAdmin,c.wildcardSsl);r.post('/:id/delete',requireAdmin,c.deleteZone);module.exports=r;
