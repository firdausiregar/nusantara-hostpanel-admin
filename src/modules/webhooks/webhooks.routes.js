'use strict';const express=require('express');const {requireAuth,requireAdmin}=require('../../middleware/auth');const {requireWorkspace}=require('../../core/security/workspace');const c=require('./webhooks.controller');const r=express.Router();r.post('/apps/:id/webhook',requireAuth,requireWorkspace,requireAdmin,c.create);r.post('/apps/:id/webhook/disable',requireAuth,requireWorkspace,requireAdmin,c.disable);module.exports=r;
