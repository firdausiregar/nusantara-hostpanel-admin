'use strict';const express=require('express');const {requireAuth,requireAdmin,requireDeveloper}=require('../../middleware/auth');const {requireWorkspace}=require('../../core/security/workspace');const c=require('./domains.controller');const r=express.Router();r.use(requireAuth,requireWorkspace);r.get('/',c.index);r.post('/',requireDeveloper,c.create);r.post('/:id/ssl-test',requireDeveloper,c.preflight);r.post('/:id/ssl-staging',requireAdmin,c.staging);r.post('/:id/ssl-sync',requireDeveloper,c.sync);r.post('/:id/ssl',requireAdmin,c.issue);r.post('/:id/settings',requireDeveloper,c.settings);r.post('/:id/delete',requireAdmin,c.destroy);module.exports=r;
