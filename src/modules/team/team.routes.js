'use strict';const express=require('express');const {requireAuth,requireAdmin}=require('../../middleware/auth');const {requireWorkspace}=require('../../core/security/workspace');const c=require('./team.controller');const r=express.Router();r.get('/invite/:token',c.acceptPage);r.post('/invite/:token',c.accept);r.use('/team',requireAuth,requireWorkspace,requireAdmin);r.get('/team',c.index);r.post('/team/invite',requireAdmin,c.invite);r.post('/team/:id/role',requireAdmin,c.role);r.post('/team/:id/remove',requireAdmin,c.remove);module.exports=r;
