'use strict';const express=require('express');const {requireAuth,requireDeveloper,requirePlatformAdmin}=require('../../middleware/auth');const {requireWorkspace}=require('../../core/security/workspace');const c=require('./logs.controller');const r=express.Router();r.use(requireAuth,requireWorkspace);r.get('/',requireDeveloper,c.index);r.get('/tail',requireDeveloper,c.tail);module.exports=r;
