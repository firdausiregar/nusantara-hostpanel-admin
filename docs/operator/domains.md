# Domains & Nginx

Domain Center mengelola reverse proxy, HTTPS redirect, canonical hostname, WebSocket, upload limit, proxy timeout, maintenance mode dan security header allowlist. Seluruh konfigurasi diuji dengan `nginx -t` sebelum reload.
